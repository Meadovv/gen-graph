export default function GraphView({ data }) {

    return (
        <div style={{
            display: 'flex',
            justifyContent: 'center',
            height: '80vh',
            width: '60%',
            padding: 10,
            borderRadius: 10,
            border: '1px solid black',
        }}>

        </div>
    )
}